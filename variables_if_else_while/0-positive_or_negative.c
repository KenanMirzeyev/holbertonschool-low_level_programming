#include <stdlib.h>
#include <time.h>
/* main
*
**positive_or_negative
*
*/
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* is_positive
	*
	** is_zero
	*
	** is_negative
	*
	*/
	return(0);
}
